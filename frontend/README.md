# Frontend (Vite + React)

## Quick start
```bash
cd frontend
npm install
npm run dev
```

It expects backend at http://127.0.0.1:8000.
If you run backend elsewhere:
```bash
# Windows (PowerShell)
$env:VITE_API_BASE="http://127.0.0.1:8000"
npm run dev
```
